import {
  StyleSheet,
  Text,
  View,
  SafeAreaView,
  StatusBar,
  TextInput,
  TouchableOpacity,
} from "react-native";
import fonts from "../constants/Fonts";
import React from "react";

export default function FilloutForm() {
  return (
    <SafeAreaView>
      <Text>FilloutForm</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});
